Замени package.json на файл из этого архива.

Если не хочешь заменять целиком, просто перенеси devDependencies из package.json внутрь своего package.json.

После этого отправь изменения в GitHub и заново запусти деплой на Vercel.
